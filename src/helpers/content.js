import {
  lightTheme,
  darkTheme,
  BREAKPOINTS,
} from "../constants/constants";
import { useSelector, useDispatch } from "react-redux";
import React, { useEffect, useRef, useState } from "react";

// export function StepsData (){
// const {currentStep, stepsTextData} = useSelector((state) => state.state);
// const screens = useRef(null);
// const theme = useRef(null);
// // if (currentStep < 6) {
// //   screens = homeScreens;
// //   theme = darkTheme;
// // } else {
// //   screens = approachScreens;
// //   theme = lightTheme;
// // }
// return <>{currentStep, stepsTextData}</>;
// }



